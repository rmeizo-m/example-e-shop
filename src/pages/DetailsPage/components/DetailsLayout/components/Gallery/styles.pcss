.component { max-width: 100%; }

.thumbs {
  display: flex;
  justify-content: space-between;
}

.thumb {
  flex: 1 1 0;
  background: #f0f0f0;
  & + & { margin-left: 8px; }
  & img { max-width: 100%; }
}

.mainImage {
  width: 100%;
}

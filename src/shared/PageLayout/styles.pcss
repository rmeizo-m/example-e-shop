@import "styles/constants.pcss";

.container {
  display: flex;
  flex-direction: column;
  min-height: 100vh;
  min-width: 320px;
  align-items: stretch;
}

.content { flex: 1 1 auto; }
.afterContent { flex: 1 1 auto; }
